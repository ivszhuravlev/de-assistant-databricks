# Reference stub only. It is intentionally not included by databricks.yml.
resources:
  jobs:
    generic_de_generator:
      name: "[${bundle.target}] Generic DE Generator"
      tasks:
        - task_key: generate
          existing_cluster_id: ${var.existing_cluster_id}
          notebook_task:
            notebook_path: ../notebooks/run_generator.py
            base_parameters:
              config_path: config/example.json
              repo_root: ${workspace.file_path}
              workspace_root: ${workspace.root_path}
              cluster_id: ${var.existing_cluster_id}
